# Placeholder scenario data for testing harness validation.
# Replace with autogenerated/curated scenarios once simulation core is ready.

metadata:
  id: "example_scenario"
  description: "Minimal H2 system for schema tests."

expected:
  energy_total_kj_mol: null
  temperature_k: null
  bond_lengths_angstrom:
    H-H: null

config_template: "../../config/template.yaml"
